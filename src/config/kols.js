// KOL roster — Demo Brief §7.
// LUCA's core dataset. Supports cross-indication tagging, sentiment, trend.
//
// NOTE: the full KOL array still lives in src/data/demoData.js (318 lines).
// It is re-exported here so the canonical import surface is `config/*`. The
// data itself will migrate when LUCA is reshaped in a later step.

export { KOL_DATA } from '../data/demoData';
